<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Educator Details</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f9fafb;
        font-family: 'Inter', sans-serif;
      }
      .container {
        max-width: 600px;
        margin: 3rem auto;
        background-color: #ffffff;
        padding: 2rem;
        border-radius: 0.5rem;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }
      .heading {
        text-align: center;
        font-size: 2rem;
        font-weight: bold;
        color: #1e40af;
        margin-bottom: 2rem;
      }
      .info-label {
        font-weight: bold;
        color: #374151;
      }
      .info-value {
        margin-bottom: 1rem;
        color: #4b5563;
      }
      .profile-image {
        margin: 1rem 0;
        border-radius: 50%;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="heading">Educator Details</h1>

      <div>
        <p class="info-label">Name:</p>
        <p class="info-value"><%= educator.name %></p>

        <p class="info-label">Email:</p>
        <p class="info-value"><%= educator.email %></p>

        <p class="info-label">Phone:</p>
        <p class="info-value"><%= educator.phoneNo %></p>

        <p class="info-label">Experience:</p>
        <p class="info-value"><%= educator.experienceYears %> years</p>

        <p class="info-label">Bio:</p>
        <p class="info-value"><%= educator.bio %></p>

        <p class="info-label">Qualifications:</p>
        <p class="info-value"><%= educator.qualifications %></p>

        <% if (educator.profileImage) { %>
        <p class="info-label">Profile Image:</p>
        <img
          src="<%= educator.profileImage %>"
          alt="Educator Image"
          class="profile-image w-32 h-32 border"
        />
        <% } %>

        <h3 class="info-label">Programs:</h3>
        <ul class="list-disc list-inside text-gray-600 mb-4">
          <% educator.programs.forEach(program => { %>
          <li><%= program.programName %></li>
          <% }) %>
        </ul>
      </div>
    </div>
  </body>
</html>
