<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
    <style>
        body {
            background-color: #f3f4f6; /* Light gray background */
        }
        .sidebar {
            width: 250px;
            background-color: #2c5e4f; /* Blue */
            color: white;
            padding: 20px;
        }
        .sidebar .logo {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 2rem;
        }
        .sidebar ul {
            list-style-type: none;
            padding: 0;
        }
        .sidebar ul li {
            margin-bottom: 1rem;
        }
        .sidebar ul li a {
            display: block;
            color: white;
            padding: 10px 15px;
            border-radius: 0.375rem; /* Rounded corners */
            text-decoration: none;
            transition: background-color 0.3s;
        }
        .sidebar ul li a:hover, .sidebar ul li a.active {
            background-color: #89bb91; /* Darker blue */
        }
        .main-content {
            padding: 2rem;
            flex: 1;
        }
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }
        .stat-card {
            background-color: #ffffff;
            padding: 1.5rem;
            border-radius: 0.5rem; /* Rounded corners */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .stat-card h3 {
            font-size: 1.25rem;
            color: #374151; /* Dark gray */
        }
        .stat-card p {
            font-size: 2rem;
            font-weight: bold;
            color: #1e1c8d; /* Blue */
        }
    </style>
</head>
<body>
    <div class="dashboard-container flex min-h-screen">
        <nav class="sidebar">
            <div class="logo">Admin Panel</div>
            <ul>
                <li><a href="/admin/dashboard" class="active">Dashboard</a></li> <!-- Added Dashboard link -->
                <li><a href="/admin/students">Students</a></li>
                <li><a href="/admin/educators">Educators</a></li>
                <li><a href="/admin/programs">Programs</a></li>
                <li><a href="/admin/queries">Queries</a></li> 
                <li><a href="/logout">Logout</a></li>
                
            </ul>
        </nav>
        
        <main class="main-content">
            <h1 class="text-3xl font-bold text-gray-800">Dashboard</h1>
            <div class="stats-container">
                <div class="stat-card">
                    <h3>Total Students</h3>
                    <p><%= totalStudents %></p>
                </div>
                <div class="stat-card">
                    <h3>Total Educators</h3>
                    <p><%= totalEducators %></p>
                </div>
                <div class="stat-card">
                    <h3>Total Programs</h3>
                    <p><%= totalPrograms %></p>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
