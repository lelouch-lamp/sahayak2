<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Student Details</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f9fafb;
        font-family: 'Inter', sans-serif;
      }
      .profile-image {
        border-radius: 50%;
      }
    </style>
  </head>
  <body>
    <div class="max-w-4xl mx-auto mt-8 p-6 bg-white rounded-lg shadow-md">
      <h1 class="text-3xl font-bold text-blue-900 text-center mb-6">
        Student Details
      </h1>

      <% if (student.profileImage && student.profileImage.trim() !== "") { %>
      <div class="flex justify-center mb-6">
        <img
          src="<%= student.profileImage %>"
          alt="Student Image"
          class="profile-image w-32 h-32 border-2 border-blue-500"
        />
      </div>
      <% } %>

      <div class="space-y-4">
        <div>
          <p class="font-semibold text-gray-700">Student ID:</p>
          <p class="text-gray-600"><%= student.studentId %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Name:</p>
          <p class="text-gray-600"><%= student.name %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Status:</p>
          <p class="text-gray-600"><%= student.status %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Program:</p>
          <p class="text-gray-600"><%= student.programId.programName %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Days of the Week:</p>
          <p class="text-gray-600"><%= student.daysOfWeek.join(", ") %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Primary Educator:</p>
          <p class="text-gray-600"><%= student.educator.name %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Secondary Educator:</p>
          <p class="text-gray-600">
            <%= student.secondaryEducator ? student.secondaryEducator.name : "None" %>
          </p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Session Type:</p>
          <p class="text-gray-600"><%= student.sessionType %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Father's Name:</p>
          <p class="text-gray-600"><%= student.fatherName %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Mother's Name:</p>
          <p class="text-gray-600"><%= student.motherName %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Blood Group:</p>
          <p class="text-gray-600"><%= student.bloodGroup %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Contact Number:</p>
          <p class="text-gray-600"><%= student.contactNo %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Alternative Contact:</p>
          <p class="text-gray-600"><%= student.alternativeContactNo %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Parent's Email:</p>
          <p class="text-gray-600"><%= student.parentEmail %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Address:</p>
          <p class="text-gray-600"><%= student.address %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Transport:</p>
          <p class="text-gray-600"><%= student.transport %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Comments:</p>
          <p class="text-gray-600"><%= student.comments %></p>
        </div>
        <div>
          <p class="font-semibold text-gray-700">Enrollment Year:</p>
          <p class="text-gray-600"><%= student.enrollmentYear %></p>
        </div>
      </div>

      <div class="flex justify-center mt-6">
        <button
          onclick="window.history.back()"
          class="px-6 py-2 bg-blue-600 text-white rounded-full hover:bg-blue-700 transition duration-200"
        >
          Back
        </button>
      </div>
    </div>
  </body>
</html>
